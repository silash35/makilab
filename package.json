{
  "name": "opensom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@opensom/eslint-config": "workspace:*"
  }
}
