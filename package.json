{
  "name": "opensom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "manager:dev": "cd packages/manager && pnpm dev",
    "manager:build": "cd packages/manager && pnpm build",
    "manager:start": "cd packages/manager && pnpm start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@opensom/eslint-config": "workspace:*"
  }
}
